server:
  port: 8080

spring:
  application:
    name: atm-lookup-service

  main:
    lazy-initialization: true

app:
  telegram-bot:
    name: test-name
    token: test-token
  atm-client:
    id: test-client
    url: https://apiws.alfabank.ru/alfabank/alfadevportal/atm-service
    connectionTimeout: 30000
    readTimeout: 5000
    keystore: ssl/apidevelopers.p12
